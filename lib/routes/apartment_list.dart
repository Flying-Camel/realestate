import 'package:flutter/material.dart';

class ApartmentList extends StatelessWidget {
  const ApartmentList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Apartment List!'));
  }
}